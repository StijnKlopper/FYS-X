using Assets.World.Generator;
using UnityEngine;

public class DungeonGenerator : MonoBehaviour, Generator
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
